package edu.tcu.rossshelashskyi.polynomialmanager

//The Term class is used to represent the terms each polynomial contains.
//It stores the term's coefficient as well as the exponents for each variable
//The constructor requires those variables in the argument
class Term(val coef: Int, val a: Int, val b: Int, val c: Int)